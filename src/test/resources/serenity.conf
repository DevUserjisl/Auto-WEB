environments {
  default {
    url.parabank = "https://parabank.parasoft.com/parabank/index.htm"
  }

}

webdriver {
  driver = chrome
  chrome {
    driver = drivers/chromedriver
  }
  timeout = 30000
}

logging {
  level = DEBUG
}

webdriver.driver=chrome
webdriver.chrome.driver=drivers/chromedriver
serenity.take.screenshots=AFTER_EACH_STEP